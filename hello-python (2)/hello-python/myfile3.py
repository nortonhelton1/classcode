Class User

def _init_(self, first_Name, last_Name):
    self.first_Name = first_name
    self.last_name = last_name
    self.addresses = []

def add_address(self, address):
    # add an address to the user
    self.addresses.append(address)


Class Address:

def _init_(self, street, city, state):
    self.street = street
    self.city = city
    self.state = state




user + User( "John", "Doe)" 
addresses =Address ("1200 Richmond Ave", "Houston", "TX")

user.addresses.append(address)
user.add_address(address("1200 Richmond Ave", "Houston", "TX"))

for address in user.addresses:
    print(Address.street)
    print(addresses.state)

class Post:
    


